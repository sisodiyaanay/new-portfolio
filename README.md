# Case Studies — Final Showcase Set

The finished Second Brain case-study trilogy, ready to share. Self-contained HTML (all
screenshots embedded); open any file directly in a browser.

## Portfolio

`index.html` — Anay Singh Sisodiya's portfolio homepage: hero, the three case studies below,
experience (SmartSense Consulting Solutions — Ostrich AI &amp; Second Brain), education,
skills and achievements. Built in the same design language as the case studies (same CSS
variables, type system and components) so the whole set reads as one piece. Links to the
three case studies below plus a downloadable `resume.pdf`. Content is sourced from the
`R1_External_Resume_Template` résumé; contact info is email + LinkedIn only (no phone).

## The case studies

| Piece | File | Clickable mockup |
|---|---|---|
| Second Brain (the developer mirror) | `second-brain-case-study.html` | `mockups/second-brain.html` |
| The Session Viewer | `session-viewer-case-study.html` | `mockups/session-viewer.html` |
| Second Brain for Leadership | `second-brain-leadership-case-study.html` | `mockups/second-brain-leadership.html` |

The three cross-link to each other and deep-link into the mockups (and
`mockups/iterations/` for the earlier prototypes), so keep this folder's layout intact.
`index.html` links to all three directly, so this folder now works as a self-contained
portfolio site — open `index.html` as the entry point.

## The requirements (`requirements/`)

- `the-case-study-overhaul.md` — the pivoting external review that defined this overhaul:
  the seven-section layout, formalized constraints, archetypes, IA/flow/wireframe artifacts,
  the failure-first testing section, and the per-piece mandates.
- `overhaul-spec.md` — that feedback translated into the exact, checkable build spec
  (section order, house rules, per-piece requirements, validation checklist).
- `iteration-rubric.md` — the two-pass improvement rubric the finals were scored against
  (pass 1: fidelity to the feedback; pass 2: craft quality).

## Provenance

- Finalized 2026-07-20 as **v5** of the overhaul line. The full working history (originals,
  v1–v5 snapshots, decision log D43–D46, iteration log) lives in
  `C:\learnings\second-brain-agents-experiment\`.
- Content promises: verbatim quotes are real and unedited (typos preserved); nothing is
  fabricated; recreations and designed edge states are labeled as such; the "100+ developers"
  figure is cleared for sharing.
